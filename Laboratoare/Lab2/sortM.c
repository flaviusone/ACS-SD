void Sortare(void* avector, size_t nr_elemente, size_t dim_element, TFComp f){

	//alocă spațiu pentru vectorul auxiliar utilizat în procesul de sortare
	//apelează funcția recursivă MSort(v, aux, n)
	//eliberează spațiul alocat pentru aux
}

void MSort(void* v,void* w,size_t nr_elemente,size_t dim_element,TFComp f){





}
